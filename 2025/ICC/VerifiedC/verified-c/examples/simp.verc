func(arr: array, i: int) -> int {
        if (i < len(arr)) {
            let temp = arr[i];
            return temp;
        } else {
            return 0;
        }
    }
EOF
{"arr": [1, 2, 3, 4, 5], "i": 0}
